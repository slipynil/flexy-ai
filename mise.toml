[tools]
python = "3.14"

[tasks.build]
description = "Установка зависимостей"
run = "pip install -r requirements.txt"

[tasks.start]
description = "Запуск проекта"
depends = ["build"]
run = "python main.py"
